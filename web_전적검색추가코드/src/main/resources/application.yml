spring:
  datasource:
    url: jdbc:mysql://localhost:3306/LOL_Community
    username: root
    password: onlyroot
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
server:
  servlet:
    session:
      timeout: 15m
    encoding:
      uri: UTF-8

file:
  upload-dir: C:/Users/JJH/Desktop/하계프로젝트/web/uploads/
  img-path: file:///C:/Users/JJH/Desktop/하계프로젝트/web/uploads/